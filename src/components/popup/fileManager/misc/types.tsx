import { FileFieldType } from "../../../rightPanel/editor/EntryEditor/FileInstance";

type Meta = {
  rowid: number;
  size: number;
  time: number;
  extension: string;
  fileName: string;
  details: FileFieldType;
};

export type { Meta };
